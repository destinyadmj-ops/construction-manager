# IMPLEMENTATION-NOTES

## Rule

- 指示に明記されていない実装判断、変更、妥協点はこのファイルへ追記して再利用できる形で残す。

## 2026-05-22 作業伝票

- `StoredDocumentKind` に `WORK_SLIP` を追加した。`REPORT` や既存種別へ流用すると、現場台帳の請求未 / 報告未判定を汚染するため分離した。
- 作業伝票の保存先は `.storage/sites/<現場>/<YYYY-MM-DD>/work-slips` とした。既存の写真・報告書と同じ日付軸に揃え、将来の履歴・フォルダ連携へ流用しやすくした。
- 現場台帳トップには大きめの「作業伝票」入口だけを追加し、既存の現場一覧 UI は崩さない構成にした。伝票管理本体は専用の `/site-ledger/work-slips` に分離した。
- 作業伝票ページでは、現場選択、Excel 追加、伝票一覧、現場詳細、写真、報告書、フォルダ管理、会計への導線を1画面にまとめた。現時点の「連携」は画面導線と月次アラートの可視化までとし、会計の自動仕訳連携までは広げていない。
- 作業伝票アップロードは `.xlsx` / `.xls` / `.xlsm` のみ許可した。要件が Excel 起点のため、画像や PDF を混在させず管理対象を明確にした。