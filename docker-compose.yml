services:
  db:
    image: postgres:16
    container_name: masterhub-db
    restart: always
    environment:
      POSTGRES_USER: masterhub
      POSTGRES_PASSWORD: masterhub
      POSTGRES_DB: masterhub
    ports:
      - "5433:5432"
    volumes:
      - masterhub_pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7
    container_name: masterhub-redis
    restart: always
    ports:
      - "6379:6379"
    volumes:
      - masterhub_redisdata:/data

volumes:
  masterhub_pgdata:
  masterhub_redisdata:
